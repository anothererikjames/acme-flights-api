$kind: http-request
url: "{{baseUrl}}/bookings/:bookingId/seats"
method: POST
headers:
  Content-Type: application/json
pathVariables:
  bookingId: string
body:
  type: json
  content: "{}"
examples: ./.resources/Reserve seat.resources/examples
order: 1000
