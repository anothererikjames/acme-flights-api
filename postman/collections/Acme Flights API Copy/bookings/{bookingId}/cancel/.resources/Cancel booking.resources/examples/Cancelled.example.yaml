$kind: http-example
request:
  url: "{{baseUrl}}/bookings/:bookingId/cancel"
  method: POST
  headers:
    - key: Authorization
      value: Bearer <token>
      description: "Added as a part of security scheme: bearer"
  pathVariables:
    bookingId: string
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 1000
